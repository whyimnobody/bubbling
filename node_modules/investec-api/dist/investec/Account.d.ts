import { Client } from "..";
import { InvestecAccount, InvestecTransaction, InvestecTransactionTransactionType, InvestecTransfer } from "../util/model";
export declare class Account implements InvestecAccount {
    private client;
    accountId: string;
    accountNumber: string;
    accountName: string;
    referenceName: string;
    productName: string;
    constructor(client: Client, account: InvestecAccount);
    getBalance(): Promise<import("../util/model").InvestecAccountBalance>;
    getTransactions({ fromDate, toDate, transactionType, }: {
        fromDate?: string;
        toDate?: string;
        transactionType?: InvestecTransactionTransactionType;
    }): Promise<InvestecTransaction[]>;
    transfer(recipients: Array<{
        account: Account;
        myReference: string;
        theirReference: string;
        amount: number;
    }>): Promise<InvestecTransfer[]>;
}
